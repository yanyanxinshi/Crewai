# 任务1：全球趋势侦察 (优化了搜索词策略)
identify_trends_task:
  description: >
    你是一个趋势猎手，现在的日期是 2025年。
    你的任务是利用 Google 搜索挖掘 {domains} 领域的全球最新流行风向。
    
    **执行步骤：**
    1. 搜索 "INStrends 2025 {domains}", "Pinterest aesthetic {domains}" 等关键词。
    2. 过滤掉去年的旧闻，只保留最近 3 个月内的热点。
    3. 思考这些趋势如何“本土化”为中国小红书用户喜欢的概念（例如将 "Mob Wife Aesthetic" 转化为 "黑帮大嫂风"）。
  expected_output: >
    一份简报，包含 3 个具体的趋势点，每个点都有：原版英文名称、本土化中文名称、核心视觉元素描述。
  agent: trend_scout

# 任务2：爆款笔记创作 (完全重写，适配 JSON 输出)
create_post_task:
  description: >
    参考《全球趋势本土化简报》，选择最适合 {domains} 的一个趋势，
    为我生成用于发布小红书的数据内容。
    
    **你不需要输出纯文本，请务必严格按照以下字段逻辑填充内容：**
    
    1. **Title (标题)**：
       - 必须包含 Emoji。
       - 必须是“点击诱饵”风格（震惊体、反问体、干货体）。
       - 长度控制在 20 字以内。
       
    2. **Content (正文)**：
       - 第一段：痛点或共鸣点引入，语气要像真人博主（禁止使用“你好，我是...”这种AI开场白）。
       - 中间段落：分点介绍趋势或干货，使用 Emoji (📍/✨) 作为列表头，**绝对禁止使用 Markdown 的星号 (** 或 *)**。
       - 结尾：引导互动（比如“评论区告诉我你也这样吗？”）。
       
    3. **Hashtags (标签)**：
       - 输出 5-8 个高热度标签，不带 # 号。
       
    4. **Image Prompts (生图提示词)**：
       - 根据文案内容，构思 3 张不同角度的图片提示词（用于 Midjourney），需包含：主体、光线、构图、胶片感/高清感描述。

  expected_output: >
    严格符合 XHSPost 结构的 JSON 数据。
  agent: xhs_creator
  context: [identify_trends_task]